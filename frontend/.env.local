# Frontend development environment variables

# API URLs for client-side requests (browser)
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_PIPELINE_URL=http://localhost:8000

# API URLs for server-side requests (Next.js API Routes)
API_URL_INTERNAL=http://server:4000
PIPELINE_URL_INTERNAL=http://pipeline:8000